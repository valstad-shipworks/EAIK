[package]
name = "eaik"
version = "0.1.0"
edition = "2021"
description = "Rust bindings for EAIK - Efficient Analytical Inverse Kinematics"
license = "MIT"

[dependencies]

[build-dependencies]
cc = "1"
pkg-config = "0.3"

[dev-dependencies]
approx = "0.5"
